[project]
name = "meetup-may"
version = "0.1.0"
description = "Ansible 101 - Getting Started."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible-core==2.18.6",
]

[dependency-groups]
dev = [
    "ansible-lint==25.5.0",
]
